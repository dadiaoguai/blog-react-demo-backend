const should = require('should')

describe('test', function () {
  it('add api should return 8 when x =2, y=2', () => {
    (2).should.be.equal(2)
  })
  it('substract should return -5 when x=2, y=3',() => {
    (2).should.be.equal(-5)
  })
})
